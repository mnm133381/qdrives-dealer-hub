/**
 * Admin Inventory tab — wraps the existing /my-listings flow so admins
 * have a dedicated tab for managing Q Drives Inventory + media + PDFs.
 */
import MyListings from '../my-listings/index';
export default MyListings;
