/**
 * Admin Launch tab — wraps the existing seller flow as the "Launch Auction"
 * destination inside the admin shell. Implementation lives in (tabs)/sell.tsx
 * and is reused as-is so we have a single source of truth.
 */
import Sell from '../(tabs)/sell';
export default Sell;
