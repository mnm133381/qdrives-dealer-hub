/**
 * Admin profile tab — reuses the dealer profile shell. The shell already
 * displays the "Q DRIVES ADMIN" badge and admin role row when role==='admin'.
 */
import Profile from '../(tabs)/profile';
export default Profile;
